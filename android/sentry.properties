defaults.org=personal-133
defaults.project=react-native
defaults.url=https://sentry.io/

# Optional: auth token for local source map uploads (do not commit). Prefer SENTRY_AUTH_TOKEN in CI.
# auth.token=
